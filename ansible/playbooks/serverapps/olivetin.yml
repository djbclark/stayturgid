---
# Own-mode olivetin adapter entry point. Invoked by control/site_contract/serverapps.py.
- name: Activate stayturgid olivetin serverapp (own mode)
  hosts: localhost
  connection: local
  gather_facts: false
  roles:
    - role: serverapp_olivetin
