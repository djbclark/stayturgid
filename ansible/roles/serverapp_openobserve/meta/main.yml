---
galaxy_info:
  role_name: serverapp_openobserve
  author: stayturgid
  description: Site Contract v1 openobserve adapter (own mode) — binary if absent, launchd site namespace.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: macOS
      versions:
        - all
dependencies: []
