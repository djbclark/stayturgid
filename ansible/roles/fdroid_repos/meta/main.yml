---
galaxy_info:
  author: stayturgid
  description: Manage F-Droid repositories on device via fdroidcl and ensure Neo Store client (with Shizuku + auto-updates per project rules)
  license: GPL-3.0
  min_ansible_version: "2.9"
  platforms:
    - name: GenericLinux
      versions:
        - all
dependencies:
  # The obtainium_apps role is the way to get Neo Store installed.
  # In fleet runs it will be included separately; here we can soft-include or document.
  # - role: obtainium_apps
  #   when: stayturgid_ensure_neo_store
  # (kept commented to avoid double-run; call order in fleet.yml controls it)
  []
