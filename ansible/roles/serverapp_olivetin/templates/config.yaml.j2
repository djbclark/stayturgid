# GENERATED by stayturgid site-sync — DO NOT EDIT (drift → site-sync exit 2)
# template: ansible/roles/serverapp_olivetin/templates/config.yaml.j2
# product_version: unknown commit: unknown
#
# Minimal own-mode OliveTin bootstrap (D5). Live config is a projection of
# product fragments + optional site user-actions (design §2) — D6 owns the
# inventory→actions purity and single-writer merge. Empty actions placeholder
# keeps the UI healthy until D6 populates stayturgid_ actions.

ListenAddressSingleHTTPFrontend: {{ serverapp_olivetin_listen }}
WebUIDir: {{ serverapp_olivetin_webui_dir }}
pageTitle: Stayturgid Fleet Control
actions: []
