# GENERATED by stayturgid site-sync — DO NOT EDIT (drift → site-sync exit 2)
# template: ansible/roles/serverapp_vector/templates/vector.yaml.j2
# product_version: own-mode site_ns={{ site_ns }}
#
# Own-mode base config: data_dir + loopback API for health. Product sources
# and sinks are separate --config files under generated/stayturgid/fragments/vector/
# (design §1.3 / site-contract §5.3 — no copy step).

data_dir: "{{ serverapp_vector_data_dir }}"

api:
  enabled: true
  address: "{{ serverapp_vector_api_address }}"
