---
# stayturgid fleet — Termux SSH targets only (no root/OS automation here).
all:
  children:
    stayturgid:
      hosts:
        s24:
          ansible_host: 100.123.218.30
          ansible_port: 8022
          ansible_user: djbclark
          ansible_python_interpreter: /data/data/com.termux/files/usr/bin/python
          ansible_ssh_private_key_file: "{{ lookup('env', 'HOME') }}/.ssh/termux_key"
          stayturgid_device_id: s24
          stayturgid_automation_mode: autojs6
      vars:
        termux_home: /data/data/com.termux/files/home
        termux_prefix: /data/data/com.termux/files/usr
