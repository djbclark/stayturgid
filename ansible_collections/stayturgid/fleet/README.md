# stayturgid.fleet

Ansible collection for the stayturgid fleet. Contains the `termux_pkg` module
(rootless Termux apt with update/upgrade recovery). Unit-tested via
`ansible-test units` (see tests/unit/); run from this directory.
