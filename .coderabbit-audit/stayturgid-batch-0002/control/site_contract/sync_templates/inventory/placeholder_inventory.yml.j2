# GENERATED by stayturgid site-sync — DO NOT EDIT.
# Host-less placeholder so this generated dir parses cleanly as an Ansible
# inventory *source*. Its only purpose is to make the adjacent group_vars/
# (product-owned taxonomy defaults) load when the site's ansible.cfg lists
# `generated/stayturgid/inventory` as a lower-precedence inventory source.
# Real hosts live in the site's own inventory/hosts.yml (which overrides these).
all:
  hosts: {}
