---
# Own-mode openobserve adapter entry point. Invoked by control/site_contract/serverapps.py.
- name: Activate stayturgid openobserve serverapp (own mode)
  hosts: localhost
  connection: local
  gather_facts: false
  roles:
    - role: serverapp_openobserve
