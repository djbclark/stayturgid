# Human actions — stayturgid fleet

Items the agent cannot complete without human help (credentials, GitHub auth, one-time pairing).

---

## AutoJs6 headless / fleet configuration API

**Filed:** https://github.com/SuperMonster003/AutoJs6/issues/553 (2026-07-08)

No supported non-UI API exists today; fleet setup uses UI automation (`enable_autojs6_shizuku.py` + `shared/autojs6_drawer_defaults.json`). Track upstream for Intent/ContentProvider or documented preference keys.
