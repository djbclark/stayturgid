---
description: Fleet health fixes must also update self-healing code
alwaysApply: true
---

# Fleet health → self-heal (mandatory)

When you fix a **fleet-health** problem (or any live device failure that
`control/bin/check_fleet_health.py` / `fleet-health.log` would surface), you **must**
also make that failure mode recoverable by existing self-heal paths — not only
by a one-shot manual action in the current session.

## Definition of done for a health fix

A health fix is incomplete until **all** of the following are true:

1. **Symptom cleared** — `python3 control/bin/check_fleet_health.py` is clean (or the
   specific host/`issues=` tag is gone) after the fix.
2. **Self-heal updated** — the same class of failure would be repaired without
   an agent re-running the manual steps, via one or more of:
   - Termux boot loop (`device/termux/boot/start-adb.sh` + `stayturgid_*.py`)
   - AutoJs6 cycle (`device/autojs6/lib/watchdog.js`, `comonitor.js`, `guard.js`)
   - Mac launchd (`control/bin/fleet_health_monitor.py`, `fire_help_monitor.py`,
     `access_monitor.py`, `adb_reconnect.py`)
   - Peer/Fire paths (`stayturgid_peer_keepalive.py`, peer bootstrap)
3. **Documented briefly** — note in the commit message (and HANDOFF/OPTIONS if
   operator-facing) *which* self-heal path now covers it.

## Checklist (run before declaring fixed)

Ask and answer in the work summary:

| Question | If no → do this |
|----------|-----------------|
| Would Termux’s 5‑min loop recover this without me? | Extend repair / timeouts / Fire `NO_LOCAL_ADB` skips |
| Would AutoJs6’s 20‑min cycle recover this? | Extend `comonitor.js` / `watchdog.js` / `guard.js` |
| Would Mac soft-health recover this if AutoJs6/Termux cannot? | Extend `fleet_health_monitor.py` (or fire-help) with a **rate-limited** heal |
| Did I only run a one-shot (`start_watchdog.py`, `enable_autojs6_*`, adb taps)? | Encode that one-shot into a self-heal path above |

## Anti-patterns (do not ship)

- Clearing `watchdog_stale` only by manually running `start_watchdog.py` and
  leaving no Mac/Termux/AutoJs6 restart path.
- Unhanging Fire by killing processes once without timeouts / `NO_LOCAL_ADB`
  guards in the scripts that hung.
- Re-enabling a11y once via Mac UI script without co-monitor / repair merge
  coverage for the next drop.
- “Fixed in session” commits that omit self-heal changes when the root cause
  can recur.

## Prefer existing layers

Do **not** invent a fourth control plane. Extend Termux-primary, AutoJs6
co-monitor, or Mac launchd. Termux boot loop must **not** call
`RunIntentActivity` (foreground steal); Mac or boot-launcher paths own
AutoJs6 process restarts.
